export const getTickers = state => state.monitoring.tickers;

export const getFilter = state => state.monitoring.filter;

export const getLoadind = state => state.monitoring.loading;

export const getError = state => state.monitoring.error;